# -*- coding: utf-8 -*-from NaoApplication import *from Features import *from naoqi import ALProxyfrom naoqi import ALBrokerfrom naoqi import ALModuleimport motionimport almathclass Stand(Features):    def __init__(self, robotProxy):        self.name = "Stand"        self.bot = robotProxy              def run(self, robotIP, feature):                         # Instanciation proxy pour la posture accroupi        print ("affichage proxy avan")        postureProxy = self.bot.gPostureProxy        print ("affichage proxy")        print postureProxy        postureProxy.goToPosture("StandInit", 0.5)        